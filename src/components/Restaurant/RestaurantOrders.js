import React from "react";

const RestaurantOrders = () => {
// pull restaurant info from auth - useAuth();
// then can make a thunk to pull all orders with that restaurant id
// make a stack of each of those orders - ref figma for what needs to be included
    return (
        <>
        <h1>Restaurant Order Page</h1>
        </>
    )
};

export default RestaurantOrders;